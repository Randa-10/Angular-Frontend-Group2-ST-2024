import { ImgstyleDirective } from './imgstyle.directive';

describe('ImgstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgstyleDirective();
    expect(directive).toBeTruthy();
  });
});
